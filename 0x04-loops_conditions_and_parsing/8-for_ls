#!/usr/bin/env bash
#for loop to display list of content in current directory Where only the part of the name after the first dash (-) is displayed
my_content=$(ls)
for file in $my_content; do
  echo "$file"|cut -d'-' -f2 
done
